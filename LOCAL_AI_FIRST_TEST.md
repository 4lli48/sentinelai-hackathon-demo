# دليل تشغيل واختبار نموذج الاستدلال المحلي (Local AI Engine)

## الهدف

يوفر SentinelAI وضع الاستدلال المحلي الكامل (Local / Sovereign AI Mode) لتنفيذ التحليلات التفسيرية على الجهاز نفسه دون الحاجة للاتصال بخدمات خارجية، مما يضمن أعلى درجات حماية الخصوصية وعزل البيانات المالية.

## خطوات الإعداد والتشغيل المحلي

1. تأكد من تشغيل خادم الاستدلال المحلي على الجهاز عبر المنفذ المحلي الافتراضي (`11434` أو المنفذ المحدد).
2. قم بتهيئة النموذج المحلي المخصص للمهمة.
3. اضبط متغيرات البيئة في ملف `.env` المحلي:

```text
SENTINEL_AI_PROVIDER=local
LOCAL_AI_MODEL=local-risk-model
LOCAL_AI_URL=http://127.0.0.1:11434/api/chat
```

4. شغّل SentinelAI وقم بتنفيذ تحليل المعاملات من بوابة البنك. في وضع `local`، ينفّذ النظام الاستدلال محلياً بالكامل، وفي حال تعذر الاتصال بالخادم المحلي يُعرض التقرير الحتمي الآمن فوراً.

## إثبات السيادة والعزل

- يظهر وسم `Local AI / الذكاء المحلي` في واجهة التحقيق دلالة على أن التحليل تم محلياً بالكامل على نفس الجهاز.
- لا تخرج أي بيانات خاصة بالحوالة أو العميل خارج النطاق المحلي للجهاز.
