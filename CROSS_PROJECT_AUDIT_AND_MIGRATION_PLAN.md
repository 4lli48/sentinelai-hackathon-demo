# SentinelAI — تدقيق مقارن وخطة انتقال قبل الدمج

**النطاق:** مقارنة مشروع AImoney الأصلي مع نسخة SentinelAI الحالية. هذا التقرير تحليلي فقط؛ لم يُنسخ ملف من المشروع الأصلي، ولم تُحذف بيانات أو جداول، ولم يُربط المشروعان في القراءة أو الكتابة.

## الحكم التنفيذي

نسخة **SentinelAI الحالية** هي الأنسب لتكون واجهة ومنطق نسخة الهاكاثون النهائية: لديها تصميم مصرفي ثنائي اللغة، محرك قرار حتمي منفصل عن تحليل الذكاء، لقطات قرار قابلة للتفسير، سيناريوهات متكررة، تقرير ومحادثة AI مقيدان بالقضية، واختبارات قائمة. مشروع **AImoney** يحتوي قيمة حقيقية في نموذج البيانات الدائم وسجل العمليات والعملاء والمراجع، لكنه ليس مصدرًا نظيفًا جاهزًا للربط المباشر؛ فهو يخلط Supabase وملف JSON محليًا وبيانات عرض ثابتة داخل المسار نفسه.

> **قرار التنفيذ اللاحق:** اختار المستخدم Supabase كمصدر بيانات SentinelAI الدائم. لذلك أُنشئت جداول منفصلة ببادئة `sentinel_` داخل مشروع Supabase نفسه، ورُحلت إليها البيانات المختارة. لا تتصل النسخة وقت التشغيل بجداول AImoney الأصلية، وتفاصيل التنفيذ في `SUPABASE_SENTINELAI_INTEGRATION_REPORT.md`.

## 1. خريطة المعمارية الحالية

| المجال | SentinelAI الحالي | AImoney الأصلي | نتيجة التدقيق |
|---|---|---|---|
| إطار الويب | React 19 + Vite + Express + tRPC | Next.js 14 + API Routes | لا يمكن نسخ routes أو الصفحات مباشرة بين الإطارين. |
| القرار | `server/sentinelEngine.ts`؛ قواعد حتمية و14 مرحلة | `lib/pipeline.ts`؛ قواعد و14 مرحلة مع كتابة بيانات داخل الخط نفسه | يجب الاحتفاظ بالمحرك الحالي فقط؛ وجود محركين سيعطي قرارات متنافسة. |
| ML | Isolation Forest تجريبي استشاري | مرحلة prediction نقطية داخل pipeline | يحتفظ بML الحالي؛ لا قيمة لنقل مرحلة prediction القديمة كقرار ثانٍ. |
| AI | تقرير ومحادثة من Case File مجمد، استدلال ذكي مُدار ثم مباشر ثم بديل حتمي | استدلال ذكي وسلسلة قديمة داخل pipeline/investigator | يحتفظ بالمزود والبدائل الحالية؛ تنقل فقط مبادئ التحقيق المفيدة. |
| صفحات العرض | نظرة عامة، بوابة تحويل، مساحة قرار، مكتب تحقيق، حالات، منهجية | لوحة، بوابة بنك، تحليل/عملية/سجل عملية | الواجهة الحالية أحدث وأنسب للعرض، ولا تُستبدل. |
| API | `sentinel.bootstrap` و`sentinel.analyze` و`sentinel.investigatorChat` عبر tRPC | 8 API routes في Next | لا تنقل API routes؛ يعاد بناء أي وظيفة معتمدة داخل tRPC فقط. |
| قاعدة البيانات | MySQL/Drizzle لتسجيل المستخدم فقط؛ بيانات نطاق الهاكاثون محلية/جلسة | Supabase/Postgres + ملف JSON محلي | توجد فجوة ديمومة نطاق في SentinelAI، لكن الربط المباشر بـSupabase سيخلق مصدرين للحقيقة. |

## 2. نتيجة تدقيق AImoney الأصلي

المشروع الأصلي يستخدم Supabase فعليًا عبر `lib/supabase.ts`، بمفاتيح URL وanon وservice role. المسار `app/api/analyze/route.ts` يقرأ أو ينشئ عميلًا في Supabase، ثم قد يستبدل سجل العميل بخط أساس من `localDb`، ثم يشغّل pipeline، ثم يكتب النتيجة محليًا ويعيد حساب التاريخ محليًا. كذلك يقوم `lib/pipeline.ts` بالقراءة من Supabase لبعض المراجع والكتابة إلى Supabase للمعاملة والتقييم والتنبيه والقضية والسجل.

هذا يعني أن النسخة الأصلية ليست Supabase فقط. إنها تستخدم **ثلاث طبقات بيانات متداخلة**: Supabase، و`data-store.json`، وبيانات demo مضمّنة في الشفرة. لذلك لا تصلح كقاعدة بيانات تشغيلية مباشرة لنسخة SentinelAI دون توحيد مسبق.

| مصدر AImoney | حالة التشغيل المرصودة | الملاحظة |
|---|---|---|
| Supabase: Customer | متصل؛ 9 سجلات | يمكن اعتباره مصدر بيانات مرشحًا للهجرة فقط بعد التحقق من المعرفات. |
| Supabase: Transaction | متصل؛ 9 سجلات | لا يطابق بالضرورة ملف JSON أو حالات العرض الحالية. |
| Supabase: CustomerHistory | متصل؛ 6 سجلات | عدد أقل من العملاء، لذلك لا يجوز افتراض تاريخ كامل للجميع. |
| Supabase: RiskAssessment / AIExplanation | متصلان؛ 9 سجلات لكل منهما | يمكن استخدامهما للتدقيق أو الترحيل الاختياري بعد مطابقة transaction ID. |
| Supabase: Alert / InvestigationCase / AuditLogEntry | متصل؛ 2 / 5 / 117 سجلاً | سجلات تشغيلية لا تنقل تلقائيًا إلى بيئة الهاكاثون. |
| Supabase: المرجعيات الديناميكية و`ai_reports` | الجداول غير موجودة حاليًا في الـREST API | الشفرة تشير إليها، لكن لا يجوز الاعتماد عليها قبل هجرة schema منفصلة. |
| `scripts/data-store.json` | موجود بحجم 122,358 بايت | مخزن محلي مستقل عن Supabase؛ هو مصدر تعارض وليس مصدرًا نهائيًا. |
| Supabase Storage / Auth | لا توجد استدعاءات تشغيلية مرصودة | لا شيء لنقله أو ربطه حاليًا. |

## 3. مصادر الحقيقة والبيانات المتعارضة

| نوع البيانات | SentinelAI الحالي | AImoney الأصلي | التعارض | مصدر الحقيقة النهائي المقترح |
|---|---|---|---|---|
| مستخدمو التطبيق | MySQL `users` وOAuth | لا يوجد Supabase Auth مستخدم | لا تعارض تشغيلي | جدول المستخدمين الحالي. |
| عملاء العرض | 5 ملفات ثابتة في `shared/sentinel.ts` | 9 عملاء Supabase + عملاء JSON + demo data | اختلاف العدد والمعرفات والتاريخ | جدول SentinelAI جديد؛ تُرحّل سجلات مختارة فقط بعد reconciliation. |
| سجل العميل | baseline ثابت داخل ملف العميل | CustomerHistory في Supabase + JSON + baseline demo | قد يختلف المتوسط والبلدان والمستفيدون | جدول history واحد في SentinelAI بعد تأكيد مستوى الثقة. |
| معاملات العرض | نتيجة جلسة محلية في `localStorage` وcache خادمي محدود | Transaction في Supabase + JSON | لا توجد مفاتيح تطابق مضمونة | جدول transactions جديد في SentinelAI عند تفعيل الديمومة. |
| القرار والمخاطر | المحرك الحتمي الحالي | pipeline الأصلي | عتبات ومؤشرات مختلفة | محرك SentinelAI الحالي فقط. |
| التقرير والمحادثة | استدلال AI على Case File مجمد + بديل واضح | تقرير AI + chat history محلي/Supabase اختياري | نماذج وprompts وسجل مختلف | خدمة AI الحالية فقط. |
| المراجع التنظيمية | كتالوج مراجع داخل shared | مراجع ثابتة وبعض جداول مرجعية مفقودة | لا يكفي النقل المباشر | كتالوج SentinelAI الحالي؛ توسعة لاحقة من مصادر متحققة. |
| نطاقات المواقع | قائمة موثوقة داخل المحرك الحالي | Website + `reference_trusted_websites` غير موجودة في الـREST | تضارب registry | جدول website_reference واحد في SentinelAI؛ يستورد بعد مراجعة. |

## 4. قرار لكل عنصر

| العنصر | القرار | السبب | التنفيذ المستقبلي الآمن |
|---|---|---|---|
| واجهة SentinelAI واللغتان والسمة والمسارات | **KEEP** | تحقق هدف الهاكاثون وتفصل صفحات العرض بوضوح. | لا تغيير. |
| محرك المخاطر الحالي | **KEEP** | مصدر قرار واحد، مختبر ومضبوط على سيناريوهات العرض. | لا نقل لقواعد pipeline الأصلية. |
| Isolation Forest الاستشاري الحالي | **KEEP** | يحقق دور ML الاستشاري الصريح. | لا يغير القرار. |
| Case File والتقرير والمحادثة الحاليان | **KEEP** | يرسلان حقائق اللقطة فقط ويفصلان القرار عن AI. | لا نقل chat الأصلي المعتمد على payload عميل. |
| سلسلة الاستدلال الحالية | **KEEP** | مُختبرة: استدلال مُدار ثم استدلال مباشر ثم بديل حتمي. | لا نقل أسماء النماذج القديمة غير المتحقق منها. |
| مبادئ عقد المحقق الأصلي | **MIGRATE** | الفصل بين الدليل والمرجع وتفسير AI مبدأ مفيد. | نقل المبادئ فقط إلى prompts الحالية، لا نسخ prompt الضخم حرفيًا. |
| نموذج بيانات AImoney | **MIGRATE / MODIFY** | يغطي معاملات وتاريخ وتنبيهات وقضايا تحتاجها الديمومة. | إعادة تمثيله في Drizzle/MySQL، لا ربطه كـSupabase runtime. |
| سجلات Supabase الأصلية | **MIGRATE** مشروط | تحمل قيمة عرض محتملة، لكن توجد سجلات ناقصة وتعارض مع JSON. | export read-only ثم staging ثم matching ثم موافقة قبل إدراج نهائي. |
| `data-store.json` الأصلي | **REMOVE من الهدف** | طبقة ملف محلي منافسة لـSupabase وتؤدي إلى تاريخ مزدوج. | يبقى أرشيف تدقيق فقط، ولا يُستورد تلقائيًا. |
| pipeline الأصلي وNext API routes | **REMOVE من الهدف** | يخلطان الإطارين ومصادر البيانات ويكرران المحرك. | لا تنقل أي route أو pipeline. |
| جداول مرجعية أصلية غير منشأة | **REBUILD** عند الحاجة | الكود يشير إلى جداول لا يعيد الـREST العثور عليها. | إنشاء schema واحد جديد في SentinelAI فقط بعد تعريف بيانات موثقة. |
| API دوام العمليات | **REBUILD** | SentinelAI الآن جلسة عرض فقط. | tRPC repositories ومخطط Drizzle من دون API ثانٍ. |

## 5. خطة الانتقال المقترحة

لا يبدأ أي تنفيذ حتى يحدد المستخدم ما إذا كانت سجلات AImoney التسع جزءًا مطلوبًا من عرض الهاكاثون. إذا كان الجواب نعم، يكون التنفيذ على مراحل قصيرة قابلة للعكس: أولًا تصميم schema SentinelAI الجديد، ثم هجرة read-only إلى staging، ثم تقرير deduplication بالمعرف والاسم والتاريخ والمبلغ، ثم موافقة على مصفوفة السجلات، ثم ترحيل نهائي إلى MySQL، ثم بناء tRPC واحد للقراءة والكتابة، ثم إيقاف أي قراءة runtime من Supabase و`data-store.json`.

```mermaid
flowchart LR
  A[AImoney Supabase / JSON<br/>مصدر ترحيل فقط] --> B[Staging + Reconciliation<br/>لا كتابة للأصل]
  B --> C[SentinelAI MySQL / Drizzle<br/>مصدر الحقيقة الوحيد]
  C --> D[tRPC واحد]
  D --> E[Risk Engine حتمي]
  E --> F[Case File مجمد]
  F --> G[محرك الاستدلال الذكي + بديل حتمي]
```

لا أوصي بإضافة Supabase إلى SentinelAI كطبقة runtime ثانية؛ ذلك سيخلق SQL وSupabase متنافسين على العميل والمعاملة والتاريخ. إذا كان الهدف هو الإبقاء على Supabase بدل MySQL، فيلزم قرار معماري صريح لإعادة منصة البيانات، وهو خارج طلب دمج آمن وبسيط ولا أوصي به لنسخة الهاكاثون.

## 6. معلومات مطلوبة قبل التنفيذ

| السؤال | لماذا هو مطلوب |
|---|---|
| هل تريد ترحيل سجلات Supabase الأصلية التسع إلى SentinelAI أم الاكتفاء بالسيناريوهات الحالية؟ | يحدد نطاق schema والهجرة والاختبارات. |
| هل بيانات `data-store.json` مجرد عرض قديم أم لها قيمة تاريخية يجب الاحتفاظ بها؟ | يمنع تكرار العملاء والمعاملات. |
| هل تقبل أن تكون MySQL الحالية مصدر الحقيقة النهائي للبيانات الدائمة؟ | يمنع إبقاء SQL وSupabase متنافسين. |
| هل تريد الاحتفاظ بسجل الرسائل والتقارير بين الجلسات؟ | يحدد الحاجة لجداول reports/messages بدل cache وlocalStorage. |

## 7. ما تم التحقق منه وما لم يُفترض

تم التحقق من وجود اتصال Supabase الأصلي ومفاتيحه ومن عدادات الجداول عبر قراءات فقط، ومن وجود طبقة JSON محلية، ومن وجود تعارضات بينها وبين الكود. لم تُقرأ بيانات حساسة أو تُعدل، ولم يُفترض أن الجداول المفقودة من REST موجودة لمجرد أن الشفرة تشير إليها. كذلك لم يُفترض أن الصفوف التسع في Supabase تطابق ملف JSON أو سيناريوهات SentinelAI؛ هذا يحتاج reconciliation قبل أي نقل.

## مراجع التدقيق المحلية

1. `/home/ubuntu/sentinelai-hackathon-demo/server/routers.ts`
2. `/home/ubuntu/sentinelai-hackathon-demo/server/sentinelEngine.ts`
3. `/home/ubuntu/sentinelai-hackathon-demo/server/sentinelAi.ts`
4. `/home/ubuntu/sentinelai-hackathon-demo/server/geminiFallback.ts`
5. `/home/ubuntu/sentinelai-hackathon-demo/shared/sentinel.ts`
6. `/home/ubuntu/sentinelai-hackathon-demo/client/src/contexts/DemoContext.tsx`
7. `/home/ubuntu/aimoney/app/api/analyze/route.ts`
8. `/home/ubuntu/aimoney/lib/pipeline.ts`
9. `/home/ubuntu/aimoney/lib/investigator.ts`
10. `/home/ubuntu/aimoney/lib/local-db.ts`
11. `/home/ubuntu/aimoney/lib/supabase.ts`
12. `/home/ubuntu/aimoney/supabase/schema.sql`
13. `/home/ubuntu/aimoney/supabase/migrations/003_ai_investigator.sql`
