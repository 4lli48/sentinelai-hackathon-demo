# تحقق انتقالات الصفحات

تم الانتقال من الصفحة الرئيسية إلى صفحة الحالات عبر شريط التنقل بعد إضافة مفتاح انتقال المسار. بقي الشريط الجانبي والشريط العلوي ثابتين، ودخل محتوى الصفحة الجديدة بحركة قصيرة من الأسفل مع تدرج شفافية، من دون تأخير ملحوظ أو فقدان للفلاتر أو بيانات الحالات.

تستغرق الحركة 220ms وتستخدم التحويل والشفافية فقط. وعند تفعيل `prefers-reduced-motion: reduce`، تُلغى الحركة بالكامل. لا يعاد تشغيل الانتقال عند تغيير قيمة الاستعلام فقط، مثل تغيير معرف عملية في الصفحة نفسها.

تم اختبار فلتر قرار **«مراجعة يدوية»** في صفحة الحالات؛ انتقل السجل من 14 إلى 8 حالات، ودخلت الصفوف المطابقة بحركة صعود وتدرج قصيرة متسلسلة. ترتبط كل بطاقة بتأخير محدود لا يتجاوز 272ms، وتُلغى الحركة عند تفعيل تفضيل تقليل الحركة.

أضيف تأكيد نجاح صغير لتحليل الذكاء بعد التحديث اليدوي الذي يعيد مصدرًا حيًا فقط. يظهر مع أيقونة نجاح ونص واضح، ثم يختفي تلقائيًا بعد 2.4 ثانية ولا يظهر عند البديل الحتمي أو فشل الطلب. تغطي اختبارات الحركة حدود تأخير البطاقات وشروط نجاح التحليل الحي.
