# تدقيق اتساق SentinelAI وSupabase

**إعداد:** فريق هندسة SentinelAI  
**المشروع المدقق:** `zijklqjjnbdxydoeasxt` (`ai money`)  
**النطاق:** جداول `sentinel_` ومسارات SentinelAI فقط؛ لا يغير هذا التدقيق جداول AImoney الأصلية.

## الخلاصة

تمت مطابقة طبقة الموقع، ومسارات tRPC، وطبقة Supabase، والبيانات الفعلية. تدفق SentinelAI التشغيلي متسق الآن: خمس ملفات عرض مكتملة فقط تدخل بوابة التحويل، و24 لقطة تحليل محفوظة تصل إلى الموقع كما هي، ولكل تشغيل تقرير واحد متطابق مع حمولة القرار المجمدة. أُصلح تعارض عملي واحد في طبقة القراءة: كانت بيانات مرحّلة ناقصة يمكن أن تظهر كعملاء قابلين للاختيار؛ ولم يعد ذلك ممكنًا.

| محور التدقيق | الحالة | النتيجة |
|---|---|---|
| عزل SentinelAI عن AImoney | سليم | كل مسارات الحفظ والقراءة تستخدم جداول `sentinel_` فقط. |
| العلاقات المرجعية | سليم | لا توجد تقارير أو رسائل أو حالات أو تنبيهات يتيمة. |
| حمولة القرار | سليم | لا اختلاف في الدرجة أو القرار بين صف التشغيل واللقطة المجمدة. |
| تقارير الذكاء | سليم | 24 تشغيلًا و24 تقريرًا؛ لا تكرار ولا اختلاف في المصدر أو الحالة. |
| قائمة عملاء البوابة | تم إصلاحه | تعرض الآن العملاء الخمسة ذوي خطوط الأساس المكتملة فقط. |
| فهارس SentinelAI | تم تحسينه | أضيفت أربعة فهارس للمفاتيح الخارجية التي يرصدها تدقيق الأداء. |

## أدلة البيانات الفعلية

| الفحص | النتيجة | التفسير |
|---|---:|---|
| عملاء SentinelAI | 9 | خمسة عملاء عرض معتمدين وأربعة سجلات مرجعية مرحّلة. |
| خطوط الأساس | 6 | جميع ملفات العرض الخمسة مكتملة؛ ثلاثة سجلات مرحّلة لا تحمل تاريخًا. |
| معاملات SentinelAI | 33 | 24 عملية محللة و9 معاملات مرجعية مرحّلة بلا تشغيل تحليل. |
| لقطات التشغيل / التقارير | 24 / 24 | تغطية تقرير واحد لكل لقطة تحليل. |
| رسائل التحقيق / التنبيهات / الحالات | 26 / 8 / 13 | جميع الروابط المرجعية سليمة. |
| ناتج `sentinel.persistedResults` في الموقع | 24 | يطابق عدد `sentinel_analysis_runs` التي تحمل لقطات قرار. |

## سلامة العلاقات والحمولات

لم يظهر أي تشغيل تحليل بلا معاملة، أو تقرير بلا تشغيل، أو رسالة بلا تشغيل، أو تنبيه أو حالة بلا معاملة. كما لم يظهر أي اختلاف بين الدرجة أو القرار المحفوظين في تشغيل التحليل وبين `result_payload` المجمد، ولم يظهر أي تكرار لمعرفات العمليات أو تقارير التشغيل.

> السجلات الناقصة الثلاثة والتسع معاملات بلا تحليل تنتمي جميعًا إلى مصدر `aimoney_supabase` المرحّل. لا تمثلها سيناريوهات العرض الخمسة المعتمدة. عولجت الفجوة بلا اختراع بيانات وبلا حذف: لا تعرض البوابة ولا يقبل مسار التحليل إلا ملفًا معتمدًا يملك متوسط تحويل موجبًا وعدد عمليات موجبًا ودولًا معتادة محفوظة.

## نتيجة تحقق الموقع

أظهرت بوابة التحويل العملاء الخمسة فقط: أحمد، خالد، ليان، محمد، ونورة. وبذلك لا يصل مستخدم الموقع إلى سجل عميل بلا متوسط أو دول معتادة أو عدد عمليات، بينما تبقى السجلات المرجعية المرحّلة محفوظة ومعزولة في Supabase بلا حذف أو تغيير.

## تحسين الأداء المنفذ

أضيف ترحيل `sentinelai_integrity_indexes` بنجاح، مع فهارس `sentinel_transactions.beneficiary_id` و`sentinel_transactions.website_id` و`sentinel_alerts.transaction_id` و`sentinel_alerts.analysis_run_id`. بعد الترحيل، لم يعد مستشار الأداء يرصد مفاتيح SentinelAI الخارجية الأربع كفهارس مفقودة؛ وقد يصنفها «غير مستخدمة» مبدئيًا حتى تتجمع حركة تشغيلية كافية، وهو سلوك طبيعي لفهارس جديدة.

## ملاحظات أمان غير محجوبة

كل جداول `sentinel_` مفعّل عليها RLS، ويصل التطبيق إليها من الخادم عبر مفتاح خدمة ولا يقدم اتصالًا مباشرًا من العميل. يعرض مستشار Supabase ملاحظات معلوماتية عن عدم وجود سياسات RLS صريحة؛ عند تفعيل RLS دون سياسة لا يمنح ذلك وصولًا عامًا تلقائيًا، لكن ينبغي إضافة سياسات موثقة إذا قُرر توفير وصول مباشر من المتصفح مستقبلًا.[1]

يرصد المستشار أيضًا دالة عامة مشتركة اسمها `rls_auto_enable()` بصلاحية `SECURITY DEFINER` قابلة للتنفيذ من أدوار عامة. هذه الدالة لا ينفذها مسار SentinelAI، لكنها تقع في نطاق المشروع المشترك وقد تؤثر في AImoney؛ لذلك لم تُعدّل ضمن تدقيق SentinelAI المعزول. يوصى بمراجعة مالك AImoney قبل تقييد تنفيذها.[2]

## الحكم النهائي

**مقبول تشغيليًا لنسخة SentinelAI الحالية.** البيانات التي يصل إليها الموقع متسقة مع Supabase ومحرك القرار والتقارير المحفوظة، ولا يوجد تعارض مرجعي أو قرار/حمولة أو تقرير/تشغيل داخل نطاق SentinelAI. بقيت فقط ملاحظات مشروع مشتركة غير محجوبة لا تُعالج تلقائيًا لحماية استقلال مشروع AImoney.

## المراجع

[1]: https://supabase.com/docs/guides/database/database-linter?lint=0008 "Supabase — RLS Enabled No Policy"
[2]: https://supabase.com/docs/guides/database/database-linter?lint=0028 "Supabase — Public Can Execute SECURITY DEFINER Function"
