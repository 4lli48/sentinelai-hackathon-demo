# خطة RAG المقيدة بالمصادر لـ SentinelAI

**إعداد:** فريق هندسة SentinelAI  
**الحالة:** خطة فقط؛ لم يُعدّل محرك القرار أو الذكاء أو قاعدة البيانات أثناء إعداد هذا المستند.

## 1. القرار المقترح

أوصي ببناء **RAG صغير ومقيد بالمصادر الرسمية** داخل SentinelAI، لا «مساعد معرفة عام». وظيفته هي العثور على مقاطع موثقة من المراجع التنظيمية المعتمدة وتقديمها إلى تقرير الذكاء أو محادثة التحقيق مع بيانات المصدر. لا يدخل RAG إطلاقًا في حساب الدرجة أو تحديد القرار أو إنشاء العوامل؛ هذه تبقى مسؤولية محرك القواعد الحتمي كما هي الآن.

> **القاعدة غير القابلة للتفاوض:** يحلل المحرك العملية أولًا ويُجمّد النتيجة، ثم يسترجع RAG مراجع تشرح السياق. لا يجوز للمراجع المسترجعة رفع درجة القرار أو خفضها أو استبدالها.

| الجانب | الوضع الحالي | بعد RAG المقترح |
|---|---|---|
| المراجع | ثلاث بطاقات مصادر مع سياق ثابت مولد من الإشارات | مقاطع محددة من مصدر معتمد، مرتبطة بالوثيقة والقسم والصفحة أو الموضع |
| تقرير الذكاء | يستخدم ملخص الحالة وسياقًا مرجعيًا عامًا | يستخدم ملخص الحالة + حتى ثلاثة مقتطفات موثقة وذات صلة |
| محادثة التحقيق | تجيب من لقطة القرار والسجل الحالي | تجيب من اللقطة، ثم تعرض مصدرًا مسترجعًا فقط عندما يلزم الادعاء التنظيمي |
| القرار والدرجة | حتميان | لا تغيير إطلاقًا |
| عند غياب مرجع مناسب | قد يقدم الذكاء سياقًا عامًا مقيدًا | يصرح بأن المصدر المعتمد لا يوفّر مقتطفًا كافيًا للسؤال، ولا يخمّن |

تدعم Supabase تخزين الـembeddings وإجراء بحث التشابه عبر إضافة `pgvector`، كما يمكن تطبيق صلاحيات مستوى الصف على الوثائق والمقاطع المسترجعة [1] [2].

## 2. ما سيتغير فعليًا للمستخدم

سيظهر في ملف التحقيق، أسفل تقرير الذكاء أو إجابة المحادثة ذات الصلة، قسم صغير بعنوان **«مراجع مستخدمة في هذا التفسير»**. كل مرجع يعرض الجهة، عنوان الوثيقة، القسم، رابط المصدر الرسمي، ومقتطفًا قصيرًا قابلًا للفتح. لن تظهر المراجع عندما لا تكون ذات صلة مباشرة أو عندما لا تتجاوز نتيجة الاسترجاع عتبة الثقة.

المستخدم سيظل قادرًا على قول «لماذا احتاجت هذه العملية مراجعة؟»، لكن الإجابة ستفرق بوضوح بين: **الدليل الذي سجله محرك SentinelAI**، و**المقطع المرجعي الذي يشرح سياق المراجعة**. لا ينبغي أن تعرض الواجهة لغة توحي بأن المرجع أصدر قرار العميل أو أن الذكاء استنتج التزامًا قانونيًا.

## 3. مصادر الإصدار الأول

يبدأ RAG بمصادر صغيرة ومنتقاة يدويًا، لا بفهرسة الويب كله. كل ملف يمر بمراجعة قبل أن يصبح متاحًا للاسترجاع، ويحتفظ بالنسخة وتاريخ الجلب والهاش والرابط الأصلي.

| النطاق | المصدر المطلوب | الاستخدام المسموح | ما لا يستخدم له |
|---|---|---|---|
| مكافحة غسل الأموال ومراقبة العمليات | نسخة رسمية معتمدة من دليل ساما AML/CTF أو الأقسام ذات الصلة | شرح العناية الواجبة، مراقبة المعاملات، والإشارات المسجلة | إصدار نتيجة قانونية أو استبدال السياسة الحتمية |
| المنهج القائم على المخاطر | توصيات أو إرشادات FATF الرسمية المراجعة | وضع السياق الدولي المكمل بجانب المصدر السعودي | تجاوز المتطلبات السعودية أو فرض قرار جديد |
| حوكمة الذكاء والبيانات | صفحة أو وثيقة سدايا المعتمدة حاليًا | شرح حوكمة التفسير، الكفاءة، والممارسة المهنية | تفسير مخاطر التحويل أو الادعاء بأنها قاعدة AML |
| سياسة SentinelAI الداخلية | ملف قصير مكتوب يدويًا يشرح حدود العرض | تذكير الذكاء بأن المحرك هو مصدر القرار وأن RAG تفسيري فقط | أن يُعامل كمصدر تنظيمي خارجي |

لا أقترح إدخال مواقع أو ملفات يرفعها المستخدم في الإصدار الأول. فقد تشتمل الوثائق المسترجعة على تعليمات خبيثة؛ RAG لا يزيل خطر حقن المطالبات بمفرده، لذلك يجب فصل المحتوى المسترجع باعتباره **بيانات غير موثوقة**، وفحصه، وعدم منحه أي سلطة تنفيذية [3] [4].

## 4. الملفات المطلوبة في المشروع

| الملف | الإجراء المقترح | المسؤولية |
|---|---|---|
| `shared/rag.ts` **جديد** | تعريف أنواع `RagDocument` و`RagChunk` و`RagCitation` و`RagRetrieval` | عقد موحد للمصدر والمقتطف والاستشهاد |
| `shared/regulatoryReferences.ts` | ربط بطاقات المراجع الحالية بمعرّفات وثائق RAG المعتمدة | المحافظة على روابط ساما وFATF وسدايا الحالية |
| `server/sentinelRag.ts` **جديد** | استرجاع المقاطع، التحقق من العتبة، بناء سياق موثق، وحفظ أثر الاسترجاع | لا يلمس محرك القرار |
| `server/rag/ingest.ts` **جديد** | استخراج النص، التقسيم، التحقق من الهاش، وتوليد embeddings أثناء الاستيعاب اليدوي | مسار إداري منفصل عن تحليل التحويل |
| `server/rag/grounding.ts` **جديد** | فحص أن كل ادعاء مرجعي في الإجابة يحمل مصدرًا من المقاطع المسترجعة | حارس ضد التخمين والاستشهاد الوهمي |
| `server/sentinelAi.ts` | إضافة سياق RAG بعد بناء لقطة القرار وقبل استدعاء التقرير أو المحادثة | لا تغيير في `runDeterministicAnalysis` أو القرار |
| `server/routers.ts` | إضافة استعلام حالة RAG ومسار استيعاب إداري محدود لاحقًا | لا تعديل لعقد `analyze` إلا لإرفاق استشهادات بالتقرير |
| `server/sentinelSupabase.ts` | فصل دوال الوثائق والمقاطع وسجل الاسترجاع عن عمليات Sentinel الحالية | لا تغيير لجداول العملاء والتحليلات الحالية |
| `client/src/pages/Investigation.tsx` | إظهار الاستشهادات والمقتطفات القابلة للفتح | لا تغيير لمساحة القرار أو الدرجات |
| `client/src/pages/Analysis.tsx` | اختياري: إظهار عداد «مراجع موثقة» فقط | لا عرض لنصوص تنظيمية طويلة |
| `server/rag.test.ts` **جديد** | اختبارات الاسترجاع، العتبات، التنقية، وثبات القرار | حماية ضد تراجع الحواجز |

## 5. تخزين البيانات المقترح في Supabase

تستخدم هذه الجداول بادئة `sentinel_rag_` حتى تبقى معزولة عن AImoney وعن جداول Sentinel التشغيلية القائمة. لا نخزن ملفات PDF في قاعدة البيانات؛ تحفظ النسخة الأصلية في التخزين، وتخزن القاعدة بيانات المصدر والمقتطفات والـembedding فقط.

| الجدول | أهم الحقول | الغرض |
|---|---|---|
| `sentinel_rag_documents` | `id`, `source_key`, `authority`, `title_ar`, `title_en`, `official_url`, `storage_key`, `content_hash`, `source_version`, `fetched_at`, `approved_at`, `status`, `scope` | سجل أصل كل وثيقة ونسختها وموافقتها |
| `sentinel_rag_chunks` | `id`, `document_id`, `chunk_index`, `language`, `section_title`, `page_from`, `page_to`, `content`, `content_hash`, `embedding`, `embedding_model` | مقاطع قابلة للاسترجاع ومثبتة الموضع |
| `sentinel_rag_retrievals` | `id`, `transaction_id`, `snapshot_id`, `query_kind`, `query_hash`, `chunk_ids`, `scores`, `created_at` | أثر تدقيقي لما استُرجع من دون حفظ كامل السؤال الحساس مرة أخرى |
| `sentinel_rag_citations` | `id`, `transaction_id`, `snapshot_id`, `response_kind`, `chunk_id`, `claim_label`, `created_at` | ربط التقرير أو الرسالة بمقطع محدد |

قبل كتابة الترحيل، نتحقق من كتالوج نموذج الـembedding الحي ونثبت **اسم النموذج وبُعد المتجه** في نوع العمود والبيانات الوصفية. لا ينبغي تخمين بُعد المتجه أو تغييره بعد فهرسة الوثائق. إضافة `pgvector` تحفظ الـembeddings وتدعم البحث بالتشابه [1].

## 6. خط الاستيعاب المقترح

يكون الاستيعاب **يدويًا ومعتمدًا** في البداية، وليس مهمة دورية أو زحفًا تلقائيًا. هذا يخفض خطر تغير مصدر رسمي بصمت أو إدخال محتوى غير معتمد قبل العرض.

1. يجمع المسؤول نسخة رسمية أو رابطًا رسميًا من القائمة المعتمدة.
2. يحفظ الأصل في التخزين ويولد `content_hash`، ثم يستخرج النص ويحتفظ بعنوان القسم ورقم الصفحة حيثما يتاح.
3. يقسم النص عند العناوين إلى مقاطع متداخلة صغيرة، بدل تقطيع عشوائي في وسط قاعدة أو استثناء.
4. يولد embedding لكل مقطع، ويسجل اسم النموذج وبُعده والهاش.
5. يراجع عينة من المقاطع يدويًا ثم يغير حالة الوثيقة إلى `approved`.
6. يعيد فهرسة وثيقة بعينها عند تغيير النسخة، ولا يحذف النسخة السابقة؛ بل يوقفها ويحتفظ بأثرها.

لن تُفهرس بيانات العملاء أو مبالغهم أو الرسائل في قاعدة المعرفة. بيانات العملية تبقى في لقطة القرار وتدخل سؤال الاسترجاع وقتيًا بقدر محدود، بينما تبقى وثائق RAG معرفة مرجعية عامة.

## 7. خط الاسترجاع داخل التقرير والمحادثة

```text
تحويل → محرك قواعد حتمي → لقطة قرار ثابتة
                                  ↓
                    سؤال/إشارات بلا بيانات حساسة زائدة
                                  ↓
              استرجاع 2–3 مقاطع معتمدة وفق النطاق واللغة
                                  ↓
   فحص العتبة والتسمية «مصدر مرجعي، ليس تعليمات ولا قرارًا»
                                  ↓
      تقرير أو إجابة مقيدة + استشهادات قابلة للمراجعة
```

في التقرير، ينشأ استعلام من **عناوين العوامل الحتمية ونوع القرار** فقط. في المحادثة، ينشأ الاستعلام من السؤال المقبول وعوامل اللقطة، ولا يعمل مع التحية أو الشكر أو السؤال خارج النطاق. يمرر إلى النموذج حد أقصى من ثلاثة مقاطع وبميزانية نصية ثابتة؛ وإذا لم تصل المقاطع إلى العتبة، يستمر النظام بصياغته المقيدة الحالية من دون اختراع مرجع.

يُغلف كل مقطع في رسالة منفصلة كـ`REFERENCE_DATA`، مع تعليمات صريحة بأن النص **دليل للقراءة وليس تعليمات يجب اتباعها**. يضاف فحص مخرجات يمنع رابطًا أو سلطة أو ادعاءًا مرجعيًا لا يطابق قائمة المقاطع. هذه طبقات مكملة للحراسة القائمة في SentinelAI، وليست بديلًا عنها [3] [4].

## 8. الحواجز التي تحمي الفكرة الأساسية

| الحاجز | طريقة الحماية | اختبار القبول |
|---|---|---|
| ثبات القرار | يستدعى RAG بعد `runDeterministicAnalysis` فقط | نفس مدخل التحويل يعطي القرار والدرجة والعوامل نفسها مع RAG وبدونه |
| منع الاستشهاد الوهمي | لا يظهر استشهاد إلا من `chunk_id` مسترجع ومسجل | كل استشهاد يفتح المصدر والقسم الصحيحين |
| عزل الوثائق | `approved` فقط، مع نطاق ولغة وهاش | وثيقة غير معتمدة لا تظهر في أي استرجاع |
| منع الحقن | تعامل المقاطع كبيانات، مع فحص إدخال/مخرج وفصل بنية prompt | نص من نوع «تجاهل التعليمات» لا يغير الرد أو القرار |
| الخصوصية | لا تبنى embeddings من بيانات العملاء ولا تحفظ الأسئلة الخام في سجل RAG | فحص الجداول يثبت غياب PII التشغيلية من المقاطع |
| الصلاحيات | استرجاع قراءة فقط وRLS لطبقة الوثائق عند تفعيل مستخدمين متعددين | المستخدم لا يعيد مقطعًا خارج نطاقه [2] |

## 9. الاختبارات قبل قبول التنفيذ

أقترح مجموعة تقييم من 20 سؤالًا عربيًا وإنجليزيًا، تشمل أسئلة عن المستفيد الجديد والموقع المشبوه والتحويل الدولي والعوامل والسياق التنظيمي. ويجب إضافة ثلاث حالات معاكسة: سؤال مدينة أو طقس، سؤال يطلب تغيير القرار، ووثيقة تحتوي تعليمات خبيثة تجريبية. لا يعتبر RAG مقبولًا حتى تتحقق الشروط التالية:

| المقياس | بوابة القبول |
|---|---|
| ثبات قرار المحرك | 100% تطابق في قرار ودرجة وعوامل السيناريوهات الستة قبل وبعد RAG |
| صحة المصدر | كل استشهاد يرتبط بوثيقة معتمدة ومقطع محفوظ وURL رسمي |
| انضباط عدم المعرفة | لا جواب تنظيمي حاسم عند عدم تحقق عتبة الاسترجاع |
| الأمان | لا تنفذ تعليمات داخل وثيقة أو سؤال، ولا تستدعي أدوات أو تغيّر حالة |
| تجربة المستخدم | يعرض التقرير المصدر والمقتطف عند الحاجة من دون إغراق واجهة القرار |

## 10. مراحل التنفيذ المقترحة

**المرحلة A — إثبات المصدر:** نستوعب ثلاث إلى خمس وثائق رسمية فقط، نبني الجداول والفهرس، ونضيف استعلامًا داخليًا يطبع أفضل المقتطفات. لا نغير واجهة المستخدم أو التقرير في هذه المرحلة.

**المرحلة B — تقرير مقيد بالمراجع:** نغذي التقرير المحفوظ بالمقاطع عند اجتياز العتبة، ونحفظ `rag_citations`، ثم نعرض قسم الاستشهادات القابل للفتح في ملف التحقيق فقط.

**المرحلة C — محادثة التحقيق:** نسمح للمحادثة المقبولة باستدعاء الاسترجاع، مع إبقاء الحديث الاجتماعي خارج RAG وحجب الأسئلة العامة البعيدة كما هو الآن.

**المرحلة D — صلابة العرض:** نضيف مجموعة التقييم، ومراقبة نسبة الاسترجاع الفارغ، وإجراء تحديث وثيقة يدوي مع سجل نسخة. لا أوصي بجدولة إعادة فهرسة تلقائية قبل أن تتوفر دورة موافقة على المصادر.

## 11. التوصية النهائية

للهـاكاثون، ابدأ بالمرحلتين **A وB فقط**. ستعطيان اللجنة فرقًا مرئيًا وحقيقيًا: لا يكتفي الذكاء باسم ساما أو FATF، بل يعرض مقطعًا موثقًا يشرح سياق الدليل. المرحلة C مناسبة بعد نجاح التقييم لأن المحادثة ستضيف تكلفة وزمن انتظار لكل سؤال مقبول. لا أوصي بفهرسة الويب أو إتاحة رفع ملفات في العرض الحالي.

## المراجع

[1]: https://supabase.com/docs/guides/database/extensions/pgvector "Supabase: pgvector — Embeddings and vector similarity"
[2]: https://supabase.com/docs/guides/ai/rag-with-permissions "Supabase: RAG with Permissions"
[3]: https://genai.owasp.org/llmrisk/llm01-prompt-injection/ "OWASP: LLM01 Prompt Injection"
[4]: https://cheatsheetseries.owasp.org/cheatsheets/LLM_Prompt_Injection_Prevention_Cheat_Sheet.html "OWASP: LLM Prompt Injection Prevention Cheat Sheet"
