# تحقق سيناريو المستفيد الجديد اليدوي

عند اختيار سيناريو **«مستفيد جديد»**، يعيد النموذج الآن مسودة يدوية فارغة: العميل والمبلغ والوجهة ونوع التحويل واسم المستفيد لا تحمل أي قيمة مسبقة. ويبقى حقل «اسم المستفيد الجديد» ظاهرًا تلقائيًا حتى يكتب المراجع الاسم بنفسه.

يغطي اختبار `bankIntake` هذه القاعدة صراحة: السيناريو المستهدف يعيد `newManualDraft()`، بينما تبقى السيناريوهات الأخرى بالقيم الجاهزة. وبمجرد اكتمال الاختيارات، يستدعي النموذج مسار التحليل المعتاد الذي يحفظ المعاملة والمستفيد الجديد كما هو موثق في `BENEFICIARY_PERSISTENCE_VERIFICATION.md`.

أثناء التحقق البصري، تعذر جلب `sentinel.bootstrap` بسبب استجابة Supabase خارجية مؤقتة `522`. أضيفت إعادة محاولة خادمية محددة بثلاث محاولات للرموز العابرة مثل 522 و429 و5xx، واختبار يحاكي التعافي بعد 522. نجحت فحوصات TypeScript و75 اختبارًا وبناء الإنتاج؛ لكن التحقق الحي للواجهة ينتظر عودة اتصال Supabase.
