# AI Acceptance Check

## الحالة المختبرة

تم تشغيل سيناريو **موقع تاجر مشبوه** حيًا من بوابة التحويل في جلسة اختبار AI الحالية، ثم فُتح ملف التحقيق الناتج.

| معيار القبول | النتيجة | الدليل |
|---|---|---|
| مصدر التقرير | ناجح | الواجهة تعرض `نموذج الذكاء الاصطناعي (AI Model)`، لا البديل الحتمي. |
| القرار الحتمي | ناجح | بقيت النتيجة `مراجعة يدوية` بدرجة `80/100`. |
| الأدلة المطابقة | ناجح | التقرير ذكر المستفيد الجديد، إشارات التصيد والشرطات، انحراف `2.74×`، تجميع الإشارات، وتجاوز سياسة الموقع. |
| استقلال ML | ناجح | التقرير يذكر صراحة أن إشارة ML استشارية ولم تحدد النتيجة. |
| الإجراء المقترح | ناجح | أوصى بإحالة الحالة إلى مراجع بشري، بما يطابق قرار المحرك. |
| سلامة التحقيق | ناجح | التقرير معروض من لقطة قرار ثابتة، ووضع المحاكاة منفصل وغير حافظ. |

## دليل الجلسة الجديدة

أنشأ التحليل الحي الجديد لقطة قرار بالمعرف `X29plZi4w_Pl`. احتفظ محرك القواعد بالنتيجة `80/100 — مراجعة يدوية`، وأظهر التحقيق مصدر التقرير `نموذج الذكاء الاصطناعي`. فقرة AI الجديدة ذكرت العوامل نفسها دون إضافة عامل جديد: مستفيد جديد (`15`)، مؤشرات موقع عالي المخاطر (`35`)، انحراف `2.74×` (`20`)، تصعيد متعدد الإشارات (`10`)، وتجاوز إلزامي لسياسة الموقع. كما أبقت إشارة ML عند `55/100` استشارية صراحةً، وأوصت بإحالة بشرية بما يطابق القرار الحتمي.

## الخلاصة

تكامل AI يعمل في الاختبار الحي كمُفسِّر مقيد بالأدلة. لم يغير الدرجة أو القرار أو سياسة التجاوز الإلزامي، ولم يضف عامل خطر خارج لقطة القرار. يبقى البديل الحتمي موجودًا كمسار احتياطي عند تعذر النموذج.
