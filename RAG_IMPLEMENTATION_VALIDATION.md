# التحقق من تنفيذ RAG في SentinelAI

## النطاق

طبقت طبقة RAG منفصلة للمراجع التنظيمية الرسمية. لا تقرأ هذه الطبقة جداول المحرك الحتمي بغرض اتخاذ القرار، ولا تكتب قرارًا أو درجة أو عامل خطر. وظيفتها الوحيدة هي تقديم سياق موثق لتقرير الذكاء ومحادثة التحقيق.

## البيانات المستوعبة

| البند | النتيجة |
|---|---:|
| وثائق معتمدة | 5 |
| مقاطع مفهرسة | 7 |
| أحداث استرجاع موثقة | 1 |
| استشهادات محفوظة | 3 |
| المصادر | ساما، FATF، سدايا |

كل وثيقة تحمل جهة مصدر ورابطًا رسميًا ونسخة وهاشًا وحالة اعتماد. يخزن كل مقطع محتواه وهاشه وبصمته المتجهة وموقعه في الوثيقة، بينما يسجل كل استرجاع المقاطع ودرجات الصلة المرتبطة بلقطة العملية.

## الضوابط ضد الهلوسة

1. لا يمرّر النظام إلى الذكاء إلا المقاطع التي تتجاوز عتبة الصلة `0.62` ومن مصدر معتمد.
2. عند عدم وجود مقطع مناسب تظهر حالة `not_found` بدل اختراع استشهاد.
3. يحجب الاستيعاب المقاطع التي تحتوي تعليمات مثل تجاهل التعليمات أو طلب كشف التعليمات الداخلية.
4. يطلب من الذكاء عدم كتابة أسماء المراجع أو `[1]` داخل نص الإجابة؛ تعرض الواجهة الاستشهادات الموثقة منفصلة وقابلة للفتح.
5. تؤكد واجهة التقرير أن الاستشهادات تشرح السياق فقط ولا تغيّر قرار المحرك.

## التحقق الوظيفي

| التحقق | النتيجة |
|---|---|
| تقرير عملية محفوظة | عرض 3 مقاطع ساما الرسمية مع الصلة والرابط الرسمي. |
| محادثة التحقيق | سؤال عن المستفيد الجديد استرجع Section 14.1 وأظهر بطاقة مصدر منفصلة. |
| سؤال عام بعيد | بقي محجوبًا بتنبيه نطاق المحادثة من دون استدعاء RAG للإجابة العامة. |
| ثبات القرار | عملية `rJ2A9qtu5tGeYe` بقيت `Approve` ودرجتها `15/100`. |
| اختبارات الانحدار | TypeScript و93 اختبار Vitest وبناء الإنتاج ناجحة. |

## الملفات الأساسية

| الملف | الدور |
|---|---|
| `server/ragSchema.sql` | جداول RAG وامتداد المتجهات ودوال المطابقة. |
| `server/ragSources.ts` | مصادر ومقاطع رسمية معتمدة. |
| `server/sentinelRag.ts` | الاستيعاب وembeddings والاسترجاع والحراسة وأثر الاستشهاد. |
| `shared/rag.ts` | عقود الاستشهادات وحالات RAG المشتركة. |
| `server/sentinelAi.ts` و`server/routers.ts` | تمرير السياق الموثق للتقرير والمحادثة فقط. |
| `client/src/pages/Investigation.tsx` | عرض الاستشهادات الموثقة في السياق والمحادثة. |
