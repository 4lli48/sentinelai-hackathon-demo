# تقرير التدقيق العميق — SentinelAI Hackathon Demo

**تاريخ التحقق:** 18 أغسطس 2026  
**النطاق:** نسخة `sentinelai-hackathon-demo` المستقلة فقط. لم يُعدَّل مشروع `aimoney` الأصلي.

## الخلاصة التنفيذية

أُجري تدقيق عميق لمسار القرار الحتمي، وطبقة RAG، ومحادثة التحقيق، وديمومة Supabase، ومسارات الواجهة، والتصدير. النتيجة أن النسخة تعمل وفق الحد المعتمد للفكرة: **محرك القواعد الحتمي وحده يصدر القرار**، بينما يظل نموذج السلوك وRAG والذكاء الاصطناعي أدوات تفسير ومراجعة مقيدة بلقطة القرار.

أصلح التدقيق سبع نقاط مؤكدة، منها سلامة المعرّفات المرجعية لـRAG، ومنع حالة واجهة مضللة أثناء تحميل السجل، وتحسين أمان صلاحية وظيفية داخل Supabase، وقابلية قراءة PDF عند احتواء السجل على حقول عربية. أُعيدت اختبارات النوع والوحدات والبناء بعد جميع التعديلات، مع نجاح **100/100** اختبارًا.

| المحور | النتيجة النهائية | الملاحظة |
|---|---|---|
| القرار | سليم | القرار والدرجة والعوامل تصدر من محرك القواعد فقط. |
| RAG | سليم ومقيد | الاسترجاع لا يغير القرار ويُسجّل من مصادره الرسمية فقط. |
| Supabase | سليم | السجل الدائم وقيود RAG المرجعية مفعّلة. |
| التصدير | سليم | تنزيل Excel وPDF جُرّب فعليًا من صفحة قرار محفوظة. |
| الواجهة | سليم | فُحصت الرحلات الرئيسة على سطح المكتب والهاتف، مع حالة تحميل واضحة. |
| الإتاحة والحركة | سليم | تمرير المحادثة يحترم تفضيل تقليل الحركة. |

## الإصلاحات المؤكدة

| # | الملاحظة المكتشفة | المعالجة المنفذة | الأثر المتحقق |
|---:|---|---|---|
| 1 | كان مسار محرك القرار قد يقرأ ملف عميل بديل عند غياب العميل المطلوب. | أصبح العميل غير المعروف يُرفض صراحةً بدل إعادة استخدام ملف آخر. | يمنع إسناد قرار إلى سجل عميل خاطئ. |
| 2 | إعادة محاولة Supabase كانت قد تشمل أخطاء دائمة. | قُصرت إعادة المحاولة على التعذرات العابرة، واستُثنيت أخطاء 400 و401 و403 و404. | تقل الطلبات غير المجدية وتصبح أسباب الفشل أوضح. |
| 3 | تمرير محادثة التحقيق لم يكن يعلن احترام تقليل الحركة. | أضيفت قاعدة مستقلة تعطل التمرير السلس عند `prefers-reduced-motion: reduce`. | سلوك أكثر ملاءمة للإتاحة. |
| 4 | سجلات استرجاع RAG والاستشهادات لم تكن محمية بعلاقة مرجعية مباشرة مع العملية المحفوظة. | أُضيف قيدا مفتاح خارجي من `sentinel_rag_retrievals` و`sentinel_rag_citations` إلى `sentinel_transactions(legacy_transaction_id)` مع `ON DELETE RESTRICT`. | لا يمكن حفظ أثر RAG يتيم أو حذف عملية لها أثر استرجاع. |
| 5 | فتح صفحة القرار قبل وصول السجل الدائم قد يعرض «لا توجد معاملة محددة» مؤقتًا. | أضيفت حالة تحميل صريحة بعنوان «جارٍ تحميل القرارات المحفوظة». | لا تُفسَّر مهلة الشبكة القصيرة كغياب بيانات. |
| 6 | PDF المختصر كان يعرض أحرفًا عربية مشوهة لأن خط PDF الافتراضي لا يحتوي محارف عربية. | يُستخدم البديل اللاتيني للحقول الثنائية، وتظهر إحالة واضحة إلى ملف Excel الثنائي اللغة عند غياب تمثيل لاتيني. | PDF مقروء، وExcel يحتفظ بالسجل العربي والإنجليزي كاملًا. |
| 7 | وظيفة `rls_auto_enable()` من نوع `SECURITY DEFINER` كانت قابلة للتنفيذ من الأدوار العامة. | سُحبت صلاحية `EXECUTE` من `anon` و`authenticated` و`PUBLIC`، مع بقاء التنفيذ لدى `postgres` و`service_role` فقط. | إزالة سطح استدعاء RPC عام غير مطلوب من التطبيق. |

## التحقق من البيانات وRAG

تستخدم طبقة RAG خمس وثائق مرجعية معتمدة وسبعة مقاطع مفهرسة من ساما وFATF وسدايا. يحفظ الاسترجاع أثره في جداول مستقلة، ويُتاح للذكاء الاصطناعي في سياق التحقيق فقط. لم يُربط أي جزء من هذه الطبقة بحساب القرار أو تجاوزات السياسة أو درجة المخاطر.

أظهر فحص قاعدة البيانات القيدين التاليين بصيغة صحيحة:

| جدول الأثر | العلاقة المرجعية | سياسة الحذف |
|---|---|---|
| `sentinel_rag_retrievals` | `transaction_id → sentinel_transactions.legacy_transaction_id` | `RESTRICT` |
| `sentinel_rag_citations` | `transaction_id → sentinel_transactions.legacy_transaction_id` | `RESTRICT` |

RLS مفعّل على جداول SentinelAI ولا توجد سياسات وصول عامة لها؛ لذلك لا يصل المتصفح مباشرة إلى جداول الأعمال، بينما تستخدم الخوادم اتصال الخدمة المحمي. هذا مناسب لنطاق النسخة التجريبية الحالي، بشرط استمرار عدم تمرير مفتاح الخدمة إلى العميل.

## التحقق الوظيفي والبصري

فُحصت صفحة البداية، وبوابة التحويل، وسجل القرار، وتفاصيل قرار محفوظ، وسجل اختيار عمليات التحقيق، وملف القضية، ولوحة الحالات، وصفحة المنهجية على سطح المكتب. كما فُحصت بوابة التحويل، وسجل القرار، وملف القضية، والحالات، والمنهجية على شاشة هاتف بعرض 375 بكسل. ظهرت رحلة الجوال منظمة، وعرضت صفحة القرار الجديدة حالة تحميل حقيقية بدل الرسالة الفارغة المؤقتة.

أُجري اختبار متصفح مستقل على العملية المحفوظة `rJ2A9qtu5tGeYe`. نُقر زرا **Export Excel** و**Export PDF** فعليًا، وتم تنزيل الملفين بنجاح من دون أخطاء في Console أو استجابات tRPC فاشلة. كما تم التحقق من سلامة أرشيف XLSX، ومن أن PDF صالح بحجم A4 ومن دون تشوهات محارف عربية.

| التحقق | النتيجة |
|---|---|
| `pnpm check` | ناجح، بلا أخطاء TypeScript. |
| `pnpm test` | 36 ملف اختبار و100/100 اختبار ناجح. |
| `pnpm build` | ناجح. |
| تنزيل Excel | ناجح: `sentinelai-rj2a9qtu5tgeye.xlsx`. |
| تنزيل PDF | ناجح: `sentinelai-rj2a9qtu5tgeye.pdf`. |
| أخطاء Console وtRPC أثناء التصدير | لا توجد. |

## ملاحظات غير مانعة

ينتج البناء تحذير حجم حزمة لأن وحدات PDF وExcel كبيرة. لا يمنع ذلك العرض أو التصدير، وقد أُثبتت العملية وظيفيًا. إذا أصبح زمن التحميل على شبكات الهاتف أولوية لاحقة، يمكن إجراء تحسين منفصل لتقسيم الحزم دون تغيير منطق SentinelAI.

ظهر في سجل شبكة سابق خطأ 401 قديم أثناء فترة تعافٍ للاتصال. لم يتكرر أثناء اختبار المتصفح النهائي، ولم يسجّل اختبار التصدير أي استجابة API فاشلة. لا يعامل التقرير هذا السجل التاريخي كعطل نشط.

## قرار القبول

> **تُقبل النسخة الحالية كنسخة عرض هاكاثون مستقلة قابلة للمراجعة.** القرار صادر من القواعد فقط، وطبقات الذكاء والسلوك وRAG تظل مقيّدة بالسجل ولا تتحكم بالنتيجة. تتطلب أي توسعة لاحقة نحو منتج مصرفي فعلي مراجعة امتثال مستقلة، وسياسات وصول مفصلة لكل دور، ومصادر بيانات إنتاجية.
