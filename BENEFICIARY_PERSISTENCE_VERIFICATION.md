# تحقق حفظ المستفيد الجديد

## النتيجة

يحفظ SentinelAI المستفيد الجديد في جدول `sentinel_beneficiaries`، ثم يحفظ معاملته في `sentinel_transactions` مع `beneficiary_id`، فتتكون علاقة قابلة للتتبع بين اسم المستفيد وتاريخه ومعاملاته. لا تُدرج العملية الجديدة تلقائيًا ضمن المستفيدين الموثوقين أو خط أساس العميل؛ تبقى العلاقة «جديدة» حتى اعتماد سياسة منفصلة لذلك.

| العنصر | حالة الحفظ | طريقة التحقق |
|---|---|---|
| اسم المستفيد | محفوظ | `display_name` في `sentinel_beneficiaries`. |
| بلد المستفيد | محفوظ | `country` في سجل المستفيد نفسه. |
| تاريخ أول ظهور | محفوظ | `first_seen_at` من وقت أول معاملة مرتبطة. |
| تاريخ الإضافة التقني | محفوظ | `created_at` تلقائيًا في Supabase. |
| المعاملات السابقة واللاحقة | محفوظة ومرتبطة | كل صف في `sentinel_transactions` يحمل `beneficiary_id`. |

## التحقق الفعلي

تمت مراجعة السجلات التشغيلية للمستفيدين في Supabase بعد الترحيل. النتيجة: **7 مستفيدين تشغيليين، 0 تاريخ أول ظهور مفقود، و0 مستفيد بلا معاملة مرتبطة**. وتحقق مثال مستفيد مُدخل من الواجهة من وجود الاسم والبلد ومعاملة مرتبطة.

## الإصلاحات المنفذة

أصبح الإنشاء الجديد يرسل `first_seen_at` من `submittedAt` في لقطة المعاملة. كما عُبئت السجلات السابقة بقيمة أول معاملة مرتبطة، أو تاريخ إنشاء السجل عندما لا توجد معاملة. ولمنع خلط أشخاص متشابهين بالاسم، أصبحت المطابقة تستخدم **الاسم والبلد معًا** بدل الاسم فقط.

> يبقى سجل معاملات المستفيد محفوظًا وقابلًا للمراجعة، لكنه لا يغير حالة «مستفيد جديد» أو خط أساس العميل تلقائيًا. هذا يمنع تحويل تكرار تجربة أو عملية موقوفة إلى علاقة موثوقة دون اعتماد صريح.
