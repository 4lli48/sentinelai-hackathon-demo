# تشغيل SentinelAI محليًا

هذه الحزمة لا تحتوي على مفاتيح الإنتاج أو مفاتيح حساباتك. تم استبعادها عمدًا حتى لا تنتقل إلى ملف مضغوط أو مستودع أو جهاز آخر من دون حماية.

## تشغيل الواجهة محليًا

افتح **مجلد الحزمة الجديد فقط** في Visual Studio Code، ثم نفّذ الأوامر التالية في الطرفية المدمجة:

```bash
pnpm install
Copy-Item LOCAL_ENV_TEMPLATE.txt .env
pnpm dev
```

في macOS أو Linux استخدم `cp LOCAL_ENV_TEMPLATE.txt .env` بدل `Copy-Item`. بعد ذلك افتح ملف `.env` وأدخل قيمك الخاصة.

| القدرة | متغيرات البيئة المطلوبة | ملاحظة |
| --- | --- | --- |
| عرض الواجهة والتنقل | لا شيء إضافي | يبدأ الموقع، لكن قد تظهر تحذيرات OAuth أو Analytics إن بقيت القيم فارغة. |
| حفظ السجل وRAG وتاريخ المستفيد | `SUPABASE_URL` و`SUPABASE_SERVICE_ROLE_KEY` | مفتاح `SERVICE_ROLE_KEY` خادمي وسري؛ لا تضعه في الواجهة أو تشاركه. |
| تحليل AI والمحادثة | `AI_API_KEY` / `GEMINI_API_KEY` | مفتاح مزود الاستدلال، أو تركه فارغاً للعمل في وضع الاستدلال المحلي بالكامل. |
| جلسات محلية | `JWT_SECRET` | أنشئ قيمة طويلة عشوائية محليًا. |
| تسجيل الدخول السحابي | متغيرات OAuth | اختيارية، ويمكن تشغيل التطبيق في وضع العرض المحلي بدونها. |

## التحقق من أنك تشغّل النسخة الحديثة

بعد البدء، يجب أن يحتوي الشريط الجانبي على **«مساحة العمليات»** كمدخل واحد، بدل روابط منفصلة بعنوان تفاصيل القرار ومكتب التحقيق والحالات والتنبيهات. داخل سجل العمليات ستجد أزرار القرار والتحقيق والحالات والتنبيهات مع تلميحات نصية عند المرور عليها.

> لا تضف ملف `.env` إلى Git أو ترسله مع ملف ZIP. يبقى `LOCAL_ENV_TEMPLATE.txt` فقط هو الملف القابل للمشاركة.
